# 让朋友使用洛琪希网站 · 公网分享指南

本指南只做一件事：**把你电脑上的洛琪希网站，开放给你指定的几位朋友使用**，
同时保持轻量（纯文字、默认不加载语音），不影响你打游戏。

> 现在用的是 **Cloudflare Tunnel**（免费、无需注册、无需实名认证），
> 不再需要花生壳。

---

## 第 0 步：访问口令

用记事本打开本机的 `config.yaml`，找到 `access` 段并设置一个不公开的强口令：

```yaml
access:
  enabled: true
  password: "请在本机填写强口令"
```

语音保持 `voice.enabled: false`（默认关），最省资源；想给朋友听声音再另说。

---

## 第 1 步：启动网站

双击 **`start.bat`**，保持窗口开着。浏览器会自动打开 `http://127.0.0.1:8321`，
输入你刚设置的本机口令，确认网站能正常回复。

---

## 第 2 步：启动穿透，拿到公网网址

双击 **`start_tunnel.bat`**，保持窗口开着。

- 稍等几秒，窗口里会出现一个网址，形如：

  ```
  https://xxxx.trycloudflare.com
  ```

- 把这个网址发给朋友即可。

> ⚠️ 两个窗口（`start.bat` 和 `start_tunnel.bat`）都要开着，朋友才连得上。

> ⚠️ **电脑重启后网址会变**——重新运行 `start_tunnel.bat`，把新网址再发一次给朋友即可。

---

## 第 3 步：朋友怎么用

1. 浏览器打开你给的 `https://xxxx.trycloudflare.com` 网址。
2. 看到登录框后，输入你单独告诉对方的访问口令。
3. 即可和洛琪希聊天。**每个朋友（每个浏览器）的聊天记录各自独立，互不串话。**
4. 右上角「↪ 退出」可退出登录。

---

## 注意事项

| 事项 | 说明 |
|---|---|
| 电脑要一直开机 | 你关机了网站就断线，这是内网穿透的固有特点 |
| 内存占用 | 纯文字约 200MB + 穿透约 30MB，打游戏几乎无感 |
| API 费用 | 朋友聊天走你的中转站 Key，有口令 + 限流保护，正常朋友用量很小 |
| 想听语音 | 先启动语音引擎（`voice-engine\GPT-SoVITS\go-api.bat`），再把 `config.yaml` 里 `voice.enabled` 改成 `true`；平时建议保持关 |
| 改口令 | 直接改 `config.yaml` 的 `access.password`，保存后重启 `start.bat` |
| 网址会变 | 每次重启电脑/穿透窗口后，`trycloudflare.com` 网址都会变，记得重发 |

---

## 常见问题

**Q：朋友打不开网页？**
确认：① 电脑已开机且两个窗口（`start.bat`、`start_tunnel.bat`）都没关；② 网址没发错（是 `start_tunnel.bat` 窗口里最新显示的那个）。

**Q：朋友登录后一直转圈 / 连接失败？**
多半是网站没启动，确认 `start.bat` 窗口开着、`http://127.0.0.1:8321` 本机能打开。

**Q：网址想固定不变，不想每次重启重发？**
免费方案网址都会变。想要永久固定网址，需要租一台便宜的云服务器（约 ¥10/月）用 frp 做穿透，可以让我帮你配。
